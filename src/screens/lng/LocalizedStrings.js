import LocalizedStrings from 'react-native-localization';
import en from './en';
import hi from './hi';

let stringslang = new LocalizedStrings({
  en: en,
  hi: hi,
});

export default stringslang;
